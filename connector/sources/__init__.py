"""
Open source connector sources package.

Each source reads from a different log/audit source inside the
customer's environment and produces pre-processed signals only.
Raw telemetry never leaves the customer environment.
"""
